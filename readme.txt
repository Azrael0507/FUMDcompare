1、项目为vs2017项目，若IDE环境有所差异，请及时升级
2、目录中未同步release和debug目录，因此需要在本地重新生成，编译时特别注意，需要选择x86的debug或者relase版本，由于x64的环境变量未设置，因此可能导致编译找不到头文件的情况
3、目录下MDconfig.ini为配置样板文件，可以拷贝入执行程序同目录，会自动读取配置
4、api目录下的动态库文件，也需要放入release以及debug程序同目录，用于运行时调用